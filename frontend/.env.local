# Do not set API_BASE_URL here for normal local dev — Next.js will use http://127.0.0.1:8000
# and your local data/processed/books.csv (empty until you add books).
# Only set API_BASE_URL when you intentionally want the frontend to talk to a remote API
# (e.g. deployed Next → deployed FastAPI).
